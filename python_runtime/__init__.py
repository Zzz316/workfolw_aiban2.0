"""AiBan Python Runner — Node-RED managed SDK adapter subprocess.

This package implements the Python side of the v2.0 runtime architecture.
It communicates with Node-RED aiban-runtime node via stdin/stdout JSON Lines.
"""

__version__ = "1.0.0"
