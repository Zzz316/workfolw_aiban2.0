# Workflow 2.0 Phase 1 测试报告

> 报告版本：v2.0.1<br>
> 基线日期：2026-07-22<br>
> 测试范围：艾班 Python Runner、Node-RED 运行时节点、帧传输与生命周期基础能力

## 1. 测试结论

Phase 1 的自动化测试已通过，Python Runner 与 Node-RED 自定义节点之间的 JSONL 通信、帧回调、配置校验和基础控制能力可以作为后续开发基线。

- 全量测试：118 / 118 通过，0 失败。
- Phase 1 Runtime 重点测试：67 / 67 通过。
  - Python Runner：27 项。
  - Node-RED 运行时节点：37 项。
  - 编辑器运行时契约：3 项。
- 真实 SDK：已观察到连续真实帧进入 Node-RED；尚未完成启停/重启全矩阵、孤儿进程检查和长稳验证。

因此，本阶段结论是“传输与运行时基础能力通过”，不等同于完整生产验收。

## 2. 测试环境

| 项目 | 实际值 |
|---|---|
| 操作系统 | Windows |
| Node.js | v24.13.0 |
| npm | 11.6.2 |
| Python | 3.9.13 |
| 自定义节点包 | `node-red-contrib-aiban-workflow@1.2.0` |
| 基线分支 | `v2.0-runtime-restart` |
| 输入基线提交 | `5b02899` |

## 3. 自动化覆盖范围

### 3.1 Python Runner

自动化测试覆盖：

- 启动握手与 `ready` 事件；
- JSONL 命令解析及异常输入处理；
- 实例启动、停止、配置更新和状态查询；
- Mock 与真实 SDK 适配层的接口边界；
- 帧数据、标签、框坐标和元数据标准化；
- 配置校验与错误事件；
- 进程退出和清理基础路径。

### 3.2 Node-RED 运行时节点

自动化测试覆盖：

- Python 子进程拉起及消息解析；
- `frame`、`ready`、`status`、`error` 等运行时事件处理；
- `msg.aiban` 帧协议输出；
- 节点输入命令及管理接口基础行为；
- 节点关闭时的子进程清理；
- 异常输出、非 JSON 输出和进程退出处理；
- 节点配置到 Python Runner 的传递。

## 4. 真实运行证据

2026-07-22 的真实运行日志表明：

- `useMock=false`；
- Python Runner 连续输出真实帧，日志可见帧号至少从 `#3` 到 `#7936`；
- 识别标签包含 `S2`、`S3`、`S4`；
- Node-RED 审计日志记录了 7,883 条 `frame_received` 事件；
- 日志包含 `group1/source1` 及推理耗时字段。

对应证据：

- `logs/frame_bridge/runtime-20260722-151307-7f59e2d67dde1743.log`
- `node-red/logs/workflow/workflow-20260722-151311-9332.jsonl`

上述证据证明真实 SDK 帧已进入 Node-RED，但不能单独证明流程终态、截图、数据库落库或长期稳定性。

## 5. 已知限制与风险

1. T01～T03 已完成六态控制器、统一控制入口和 restart/PID 回收修复；自动化已验证第二次 READY、新 session、后续帧、观察结束存活以及旧 PID 退出后只有一个替换进程。
2. 回归后 Windows 扫描未发现 AiBan Runner 进程，但真实 SDK 下的启动、停止、重复启动、重启、异常退出、Deploy、删除节点和 Node-RED 关闭全矩阵仍需 T04 验收。
3. 真实编辑器按钮操作仍需在现场 Node-RED 页面完成人工验收。
4. 尚未完成 24 小时稳定性验收。

## 6. 验收判定

| 验收项 | 状态 | 说明 |
|---|---|---|
| Python Runner 自动化 | 通过 | 27 / 27 |
| Node-RED 运行时节点自动化 | 通过 | 37 / 37 |
| 编辑器运行时契约 | 通过 | 3 / 3 |
| 全量回归 | 通过 | 118 / 118 |
| 真实 SDK 帧进入 Node-RED | 通过 | 有 2026-07-22 日志证据 |
| 统一真实状态控制 | 通过 | T01、T02，六态和三入口统一 |
| 重启持续存活 | 通过 | T03，第二次 READY 后继续出帧且 Runner 存活 |
| 孤儿进程与长稳 | 待验证 | T04、T18、T22 |

Phase 1 自动化基线验收通过；生产生命周期验收在后续里程碑继续完成。
